# 🔧 Решение проблем с админ панелью

## Проблема: Не могу войти как админ

### Шаг 1: Проверьте консоль браузера
1. Откройте сайт
2. Нажмите **F12** (или **Ctrl+Shift+I** / **Cmd+Option+I** на Mac)
3. Перейдите на вкладку **Console**
4. Нажмите кнопку "🔐 Вход"
5. Проверьте сообщения в консоли:
   - Должно появиться: `Login button clicked, showLoginModal will be set to true`
   - Затем: `LoginModal rendered`

### Шаг 2: Очистите кэш браузера
**На Render.com файлы могут кэшироваться!**

#### Chrome/Edge:
1. Нажмите **Ctrl+Shift+Delete** (или **Cmd+Shift+Delete** на Mac)
2. Выберите "Изображения и другие файлы, сохраненные в кеше"
3. Нажмите "Удалить данные"
4. Обновите страницу: **Ctrl+F5** (жесткая перезагрузка)

#### Firefox:
1. Нажмите **Ctrl+Shift+Delete**
2. Выберите "Кеш"
3. Нажмите "Удалить сейчас"
4. Обновите: **Ctrl+F5**

### Шаг 3: Проверьте что файлы загружены
В консоли браузера (F12 → Network):
1. Обновите страницу
2. Найдите файлы:
   - `admin-components.js` - должен быть 200 OK (~54KB)
   - `app.js` - должен быть 200 OK (~27KB)
3. Если файлы 304 (Not Modified), нужно очистить кэш

### Шаг 4: Проверьте JavaScript ошибки
В консоли (F12 → Console) не должно быть красных ошибок.

Типичные ошибки:
- ❌ `LoginModal is not defined` → файл admin-components.js не загрузился
- ❌ `Cannot read property 'useState' of undefined` → React не загрузился
- ❌ `Unexpected token <` → проблема с Babel

### Шаг 5: Проверьте API endpoint
Откройте в новой вкладке:
```
https://ваш-сайт.onrender.com/api/teams
```

Должен вернуться: `[]` (пустой массив)

Если вернулась ошибка 404 или HTML страница - проблема с сервером.

### Шаг 6: Тест логина через консоль
В консоли браузера (F12 → Console) выполните:

```javascript
fetch('/api/admin/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ login: 'admin777', password: '@dmin1122!' })
})
.then(r => r.json())
.then(data => console.log('Login result:', data));
```

Должен вернуться:
```json
{
  "success": true,
  "sessionId": "длинная строка..."
}
```

### Шаг 7: Redeploy на Render
Если ничего не помогло:
1. Зайдите в Render Dashboard
2. Найдите ваш сервис
3. Нажмите **Manual Deploy** → **Deploy latest commit**
4. Дождитесь завершения деплоя
5. Очистите кэш браузера и попробуйте снова

## Распространенные проблемы

### Кнопка "Вход" не реагирует
**Причина:** JavaScript не загрузился или есть ошибка

**Решение:**
1. F12 → Console - проверьте ошибки
2. F12 → Network - проверьте что все файлы загрузились (200 OK)
3. Очистите кэш: Ctrl+Shift+Delete
4. Жесткая перезагрузка: Ctrl+F5

### Модальное окно не открывается
**Причина:** Файл admin-components.js не загрузился

**Решение:**
1. Проверьте в консоли: `typeof LoginModal`
   - Должно быть: `"function"`
   - Если `"undefined"` → файл не загрузился
2. Проверьте URL файла: `/admin-components.js` (должен вернуть JS код)
3. Redeploy на Render

### После ввода логина/пароля ничего не происходит
**Причина:** Проблема с API запросом

**Решение:**
1. F12 → Network → фильтр XHR
2. Нажмите "Войти"
3. Должен появиться запрос `/api/admin/login`
4. Проверьте Response:
   - ✅ `{"success": true, "sessionId": "..."}` → логин успешен
   - ❌ `{"error": "Invalid credentials"}` → неверный логин/пароль
   - ❌ Ошибка 500 → проблема с сервером

### Вход успешен, но вкладка "Админка" не появилась
**Причина:** Состояние не обновилось

**Решение:**
1. Откройте консоль (F12)
2. Выполните: `localStorage.getItem('adminSessionId')`
3. Если вернулась длинная строка - сессия сохранена
4. Обновите страницу (F5)
5. Вкладка "⚙️ Админка" должна появиться

## Проверка работы сервера (для Render)

### Через Dashboard:
1. Откройте Render Dashboard
2. Найдите ваш сервис
3. Проверьте логи (Logs)
4. Должно быть:
   ```
   🚀 GNL League server running on http://localhost:3000
   📊 Using W3Champions matches API
   ```

### Через Shell (Render):
1. В Render Dashboard → Shell
2. Выполните:
   ```bash
   curl http://localhost:3000/api/teams
   ```
3. Должен вернуться: `[]`

## Контакты для поддержки

Если ничего не помогло, пришлите:
1. Скриншот консоли браузера (F12 → Console)
2. Скриншот Network (F12 → Network)
3. URL вашего сайта на Render
